#pragma once
#include <string>
#include <vector>

using namespace std;

void ShowPlaneOverlay(const string& bannerText, int yOffset = 0);

void ShowMultiplePlaneOverlays(const vector<string>& bannerTexts);

